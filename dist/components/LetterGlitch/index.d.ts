import { h } from 'preact';
export declare const LetterGlitch: ({ glitchColors, glitchSpeed, centerVignette, outerVignette, smooth, characters }: {
    glitchColors: string[];
    glitchSpeed: number;
    centerVignette: boolean;
    outerVignette: boolean;
    smooth: boolean;
    characters: string;
}) => h.JSX.Element;
