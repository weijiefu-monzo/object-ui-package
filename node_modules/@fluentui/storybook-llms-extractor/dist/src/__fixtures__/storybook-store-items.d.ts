import type { StorybookStoreItem } from '../types';
/**
 * Storybook store items fixture, contains metadata and stories for components and pages.
 */
export declare const storybookStoreItems: StorybookStoreItem[];
